# Venice.ai configuration - Required for AI agent authentication
VENICE_API_KEY=your-venice-api-key-here

# Convex configuration - Required for database and agent deployment
CONVEX_DEPLOYMENT=your-convex-deployment-here
VITE_CONVEX_URL=your-convex-deployment-url-here

# Setup Instructions:
# 1. Get your Venice.ai API key from https://venice.ai
# 2. Run 'npx convex dev' to set up your Convex deployment
# 3. Set the VENICE_API_KEY in your Convex deployment: npx convex env set VENICE_API_KEY your-key-here
# 4. Copy the CONVEX_DEPLOYMENT and VITE_CONVEX_URL values from the generated .env.local file

# Important! Never commit your `.env` file to version control as it contains sensitive information.